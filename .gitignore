# node_modules and npm logs
node_modules
.npm-debug.log

# environment files
.env

# build and dist directories
dist

# Logs
logs/
*.log

# dev-only self-signed TLS material (see certs/generate-dev-certs.sh) —
# never commit a private key, even a throwaway dev one
certs/*.pem