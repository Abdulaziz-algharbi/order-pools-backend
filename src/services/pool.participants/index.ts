import poolParticipantModel from './pool.participant.model';
import poolParticipantController from './pool.participants.controller';
import poolParticipantRoutes from './pool.participants.routes';

export default {
  model: poolParticipantModel,
  controller: poolParticipantController,
  routes: poolParticipantRoutes,
};
